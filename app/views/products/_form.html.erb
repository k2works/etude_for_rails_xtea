<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :PD_CODE %>
    <%= form.text_field :PD_CODE, id: :product_PD_CODE %>
  </div>

  <div class="field">
    <%= form.label :PD_NAME %>
    <%= form.text_field :PD_NAME, id: :product_PD_NAME %>
  </div>

  <div class="field">
    <%= form.label :PURCHASE_PRICE %>
    <%= form.text_field :PURCHASE_PRICE, id: :product_PURCHASE_PRICE %>
  </div>

  <div class="field">
    <%= form.label :SALES_PRICE %>
    <%= form.text_field :SALES_PRICE, id: :product_SALES_PRICE %>
  </div>

  <div class="field">
    <%= form.label :PD_UNIT %>
    <%= form.text_field :PD_UNIT, id: :product_PD_UNIT %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
